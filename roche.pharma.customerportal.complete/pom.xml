<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
    
    <parent>
        <artifactId>roche-pharma-customerportal</artifactId>
        <groupId>roche.pharma.customerportal</groupId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    
	
	<groupId>roche.pharma.customerportal.complete</groupId>
	<artifactId>roche-pharma-customerportal-complete-package</artifactId>
	<packaging>content-package</packaging>
	<name>
		Roche Pharma Customerportal Complete :: Package
	</name>
	<description>Embedding all Roche Pharma Customerportal CQ packages into one big package</description>

	<properties>
        <!-- this is an artificial package root, because this package only consists out of packages -->
        <package.root>/apps/roche/pharma/customerportal/complete</package.root>
    </properties>
	
	<build>
        <plugins>
            <plugin>
                <groupId>com.day.jcr.vault</groupId>
                <artifactId>content-package-maven-plugin</artifactId>
                <configuration>
			          <instructions>
			           <!-- Import any version of javax.inject to allow running on multiple versions of AEM -->
			            <Import-Package>javax.inject;version=0.0.0,*</Import-Package>			
			             <Sling-Model-Packages>
			                          com.roche.pharma.customerportal.core
			              </Sling-Model-Packages>
			         </instructions>
                    <subPackages>
                        <subPackage>
                            <!-- include every package in dependencies -->
                            <type>content-package</type>
                            <filter>true</filter>
                        </subPackage>
                    </subPackages>
                </configuration>
            </plugin>

            <!-- checks all xml files (within view projects) for well-formedness -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>xml-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
	
	<!-- list all the packages as dependencies here, which should be embedded in this package -->
    <dependencies>

        <dependency>
            <artifactId>roche-pharma-customerportal-core-package</artifactId>
            <groupId>roche.pharma.customerportal.core</groupId>
            <version>${project.version}</version>
            <type>content-package</type>
        </dependency>
		
		<dependency>
            <artifactId>roche-pharma-customerportal-ui-package</artifactId>
            <groupId>roche.pharma.customerportal.ui</groupId>
            <version>${project.version}</version>
            <type>content-package</type>
        </dependency>
		
		<dependency>
            <artifactId>roche-pharma-customerportal-ui-clickdummy</artifactId>
            <groupId>roche.pharma.customerportal.ui</groupId>
            <version>${project.version}</version>
            <type>content-package</type>
        </dependency>
     
        <dependency>
            <artifactId>roche-pharma-customerportal-config-package</artifactId>
            <groupId>roche.pharma.customerportal.config</groupId>
            <version>${project.version}</version>
            <type>content-package</type>
        </dependency>
					
    </dependencies>
	
</project>